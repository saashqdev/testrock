// The layout.tsx handles rendering the templates table
// This page.tsx exists to make the route valid but doesn't render anything
// since the layout already provides all the content
export default function EntityTemplatesIndexPage() {
  return null;
}
